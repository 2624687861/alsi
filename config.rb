require 'compass/import-once/activate'
# Require any additional compass plugins here. 导入其他compass插件

# Set this to the root of your project when deployed:  部署时把一下路径设为工程根目录
http_path = "/"
css_dir = "css"
sass_dir = "sass"
images_dir = "img"
javascripts_dir = "js"
fonts_dir = "fonts"

# You can select your preferred output style here (can be overridden via the command line): 根据喜好输出样式
# output_style = :expanded or :nested or :compact or :compressed
output_style = :compressed

# To enable relative paths to assets via compass helper functions. Uncomment: 打开资源相对路径功能 去掉#
# relative_assets = true

# To disable debugging comments that display the original location of your selectors. Uncomment: 输出时关闭注释 去掉#
# line_comments = false


# If you prefer the indented syntax, you might want to regenerate this
# project again passing --syntax sass, or you can uncomment this:
# preferred_syntax = :sass
# and then run:
# sass-convert -R --from scss --to sass sass scss && rm -rf sass && mv scss sass
